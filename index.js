import React from 'react';
import { render } from 'react-dom';
import './style.css';

const App = props => (
  <pre>
  {`
    Rogmor

    Will be soon there is the brand old MMO RPG released

    off courese this is going to be ultimate program

    good question is Can I connect this repot to my git ?  
  
  
  `}
  </pre>
)
render(<App />, document.getElementById('root'));
